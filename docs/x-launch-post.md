# X launch post — @PortaldotDevKit (max 280 chars, intro-focused)

Post from `@PortaldotDevKit`. Attach `docs/pdk-launch-ad.mp4` (30s
square). Text pushed close to 280-char limit — full introduction,
non-technical, no install / no winner badge / no links.

---

## Version A — feeling-first (262 chars) — RECOMMENDED

```
Introducing PDK — the Portaldot Dev Kit.

If you have ever tried to build on Portaldot, you know the feeling. A transaction fails, the chain gives back a code you can't read, and you are stuck before you have even started.

PDK is built for exactly that moment.
```

## Version B — wall metaphor (271 chars)

```
Introducing PDK — the Portaldot Dev Kit.

Every Portaldot builder eventually hits the same wall — a failed transaction, a code you can't read, no way forward. PDK sits in your terminal and turns that wall into a clear answer with steps you can actually follow.
```

## Version C — plain description (268 chars)

```
Introducing PDK — the Portaldot Dev Kit.

Building on Portaldot means running into errors the chain never really explains. PDK turns those dead-ends into readable names and fix steps, right there in your terminal. Nothing to configure. Install, run, done.
```

---

## Posting checklist

- [ ] Sign in as `@PortaldotDevKit`
- [ ] Upload `docs/pdk-launch-ad.mp4` as media
- [ ] Paste Version A (or B / C)
- [ ] Post
- [ ] Pin the tweet
- [ ] DM Titus — "here's the launch, ready when you want to repost 🙏"

---

## Notes

- All three fit under 280 with buffer for a link if you want to add `portaldot-pdk.vercel.app` later
- A leans on the shared feeling — most repost-friendly
- B uses one metaphor (wall) — memorable, quotable
- C is the most descriptive — closest to a normal product intro
