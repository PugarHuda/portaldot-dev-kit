# X bio update + Discord outreach

Two drafts. First is the new bio for `@PortaldotDevKit`. Second is the
DM Titus should get about creating a dedicated `#pdk` channel inside
Portaldot's official Discord.

---

## X bio — updated (155 chars, fits 160-char X bio cap)

Paste this into `@PortaldotDevKit` bio field:

```
Portaldot errors, decoded. 14-command CLI (Python) + TypeScript companion in progress. 🏆 1st · Builder Tools S1 · @Portaldot
```

Alt (140 chars, tighter):

```
Portaldot errors, decoded. Python CLI + TypeScript companion. 🏆 1st Builder Tools · Portaldot S1 · @Portaldot
```

Website field: `portaldot-pdk.vercel.app`
Location: `terminal, mostly`

---

## Discord outreach — DM to Titus about #pdk channel

Send after progress-update tweet is posted.

```
Hey Titus 👋

Since PDK is live on PyPI + npm-alpha now and getting community traction, wondering if we can spin up a dedicated #pdk channel inside the official Portaldot Discord?

Proposal:
→ I moderate + handle first-response for pdk-related questions
→ Weekly "what did we learn this week" post (new KB entries, runtime notes)
→ Community KB PR reviews get 24-48h SLA
→ Cross-link with the main #dev-support channel so it's discoverable

Keeps Portaldot's Discord as the central builder home instead of splitting the audience into a standalone server. Same pattern foundry-rs, wagmi etc use.

Let me know what you think — happy to set it up whenever works for you 🙏
```

---

## Portaldot mention checklist

Two places to add `@Portaldot` mention going forward:

- [ ] X bio (see above)
- [ ] Progress update tweet — soft mention if reposting → `.@Portaldot`
- [ ] Any milestone tweet (alpha.2 ship, alpha.3 signing, etc.)
- [ ] Pinned reply on the launch tweet with links + `cc @Portaldot`
