# DM to Titus — progress update

Send after the X progress-update tweet is posted. Short, factual, links.

---

## DM text

```
Progress on PDK — posted the update on X: [paste tweet link here]

v0.2 TypeScript companion (pdk-ts) scaffolding is in the repo now, alongside the Python core:
→ github.com/PugarHuda/portaldot-hackathon-2026-pdk-AmpunBang/tree/master/pdk-ts

Alpha.1 lands doctor + accounts + version — real chain queries, read-only, so contributors can experiment safely. Signing (send / seed) lands in alpha.3 next week.

Would love a repost when you get a chance 🙏
```

---

## Alt short version (if Titus prefers punchier DMs)

```
Progress update posted on X: [tweet link]

pdk-ts (v0.2 TypeScript companion) scaffold is live in the repo — alpha.1 covers doctor + accounts + version, signing in alpha.3.

Repost when you can 🙏
```
